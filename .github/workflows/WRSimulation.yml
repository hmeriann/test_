name: Simple WR
on:
  workflow_dispatch:

jobs:
  define-matrix:
    name: Check if feature branch exists and define matrix
    runs-on: ubuntu-latest
    outputs:
      branches: ${{ steps.branches.outputs.branches }}

    steps:
      - id: branches
        run: | 
          echo 'branches=["main", "feature"]' >> "$GITHUB_OUTPUT"

  configure:
    name: Configure mount and download benchmark data
    needs: 
      - define-matrix
    runs-on: ubuntu-latest

    steps:
      - run: exit 0
      
  build:
    name: Build DuckDB versions and link the benchmarks
    needs: 
      - define-matrix
      - configure
    strategy:
      matrix:
        branch: ${{ fromJSON(needs.define-matrix.outputs.branches) }}
    runs-on: ubuntu-latest
    outputs:
      build_main: ${{ steps.successful_build_main.outputs }}
      build_feature: ${{ steps.successful_build_feature.outputs }}

    steps:
      - name: Build old and current
        shell: bash
        run: |
          echo ${{ matrix.branch }} 🐣
          # if [ ${{ matrix.branch }} == 'feature' ]; then
          #   exit 1
          # fi
          
  branch-matrix:
    name: Create a new matrix with built branch names
    if: always()
    needs: 
      - define-matrix
      - configure
      - build
    runs-on: ubuntu-latest
    outputs:
      build: ${{ steps.builds.outups.build }}
    steps:
      - id: builds
        run: |
          # echo "build=${{ toJSON(needs.build.outputs.builds) }}" >> "$GITHUB_OUTPUT"
          # set -eux
          # DATA=$(echo ${{ needs.build.outputs.builds }} | jq -c '. | map(split("/")[0]) | unique')
          # echo "build=$DATA" >> "$GITHUB_OUTPUT"
          echo "main result: ${{ needs.build['main'].result }} 1️⃣"
          echo "feature result: ${{ needs.build['feature'].result }} 2️⃣"
          if [[ needs.build['main'].result != 'success' && needs.build['feature'].result == 'success' ]]; then
            echo 'build=["feature"]' >> $GITHUB_OUTPUT
          elif [[ needs.build['main'].result == 'success' && needs.build['feature'].result == 'success'  ]]; then
            echo 'build=["main", "feature"]' >> $GITHUB_OUTPUT
          elif [[ needs.build['main'].result == 'success' && needs.build['feature'].result != 'success'  ]]; then
            echo 'build=["main"]' >> $GITHUB_OUTPUT
          else
            exit 1
          fi
            
  run-tests:
    name: Run Regression Tests
    if: always()
    needs: 
      - define-matrix
      - configure
      - build
      - branch-matrix
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        branch: ${{ fromJSON(needs.branch-matrix.outputs.build) }}
        test: ['one', 'two', 'three', 'four']
        
    steps:
          
      - name: Run Regression Test
        continue-on-error: true
        shell: bash
        run: |
          echo ${{ matrix.branch }} ⏩️ ${{ matrix.text }} ⏹️
          
  collect-issues:
    name: Collect failures
    needs: 
      - define-matrix
      - configure
      - build
      - run-tests
    if: always()
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    strategy:
      matrix: 
        branch: ${{ fromJSON(needs.define-matrix.outputs.branches) }}

    steps:
      - name: Collect issues on Benchmarks
        if: contains(github.ref_name, 'main')
        run: |
          # collect issues on benchmarks runs
          echo "touch ~/previous_failed_${{ matrix.branch }}.txt"
      
  file-issue:
    name: File Issue
    needs: 
      - define-matrix
      - configure
      - build
      - run-tests
      - collect-issues
    if: always()
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: File issue on preparation steps
        if: |
            contains(github.ref_name, 'main') && 
            (needs.configure.result != 'success' || 
            needs.build.result != 'success')
        run: |
          echo "Failed to run Weekly Regression Benchmarks"

      - name: Send issue to gh
        run: |
          echo "REGRESSIONS DETECTED"
